server.port=8443
spring.application.name=laboratorio1

# --- Esto se queda igual ---
spring.datasource.url=jdbc:postgresql://db:5432/laboratorio1
app.jwt.secret=esta_es_una_clave_muy_secreta_y_larga_para_cumplir_con_los_256_bits

# --- Solo agregas esto para habilitar la seguridad en el 8080 ---
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=${SSL_PASSWORD}
server.ssl.key-store-type=PKCS12
server.port=8080
spring.application.name=demo
#Cambiar esto
spring.datasource.url=jdbc:postgresql://localhost:5432/laboratorio
spring.datasource.username=postgres
spring.datasource.password=martin

spring.datasource.driver-class-name=org.postgresql.Driver

# Esto te mostrará en la consola las queries que ejecuta JdbcTemplate
logging.level.org.springframework.jdbc.core.JdbcTemplate=DEBUG
# Esto te mostrará los errores detallados de PostgreSQL (Triggers fallando, etc.)
logging.level.org.springframework.jdbc.datasource.DataSourceTransactionManager=DEBUG
# Entorno de desarrollo
app.jwt.secret=app.jwt.secret=esta_es_una_clave_muy_secreta_y_larga_para_cumplir_con_los_256_bits_exigidos


# Entorno de entrega
# app.jwt.secret=${JWT_SECRET_KEY}